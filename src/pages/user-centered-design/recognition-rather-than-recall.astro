---
import PrincipleLayout from '../../layouts/PrincipleLayout.astro';
import data from '../../data/user-centered-design.json';
import ContextualCues from '../../components/use-cases/user-centered-design/recognition-rather-than-recall/ContextualCues.astro';
import PreFilledInformation from '../../components/use-cases/user-centered-design/recognition-rather-than-recall/PreFilledInformation.astro';
import RecognitionBasedNavigation from '../../components/use-cases/user-centered-design/recognition-rather-than-recall/RecognitionBasedNavigation.astro';
import RecognitionOverCommands from '../../components/use-cases/user-centered-design/recognition-rather-than-recall/RecognitionOverCommands.astro';
import VisibleOptions from '../../components/use-cases/user-centered-design/recognition-rather-than-recall/VisibleOptions.astro';
import PrincipleNav from '../../components/PrincipleNav.astro';

const principle = data.principles.find((p) => p.id === 'recognition-rather-than-recall')!;
const baseUrl = import.meta.env.BASE_URL;
const sectionPath = '/user-centered-design';
const sectionName = 'User-Centered Design';
---

<PrincipleLayout
  title={`${principle.title} / ${sectionName}`}
  principle={principle}
  categoryColors={data.categoryColors}
  sectionName={data.title}
  sectionPath={sectionPath}
>
  <ContextualCues />
  <PreFilledInformation />
  <RecognitionBasedNavigation />
  <RecognitionOverCommands />
  <VisibleOptions />
  <PrincipleNav
    baseUrl={baseUrl}
    currentPrincipleId={principle.id}
    sectionPath={sectionPath}
    allPrinciples={data.principles}
  />
</PrincipleLayout>
