---
interface Props {
  variant: 'do' | 'dont' | 'warn' | 'info';
}

const { variant } = Astro.props;
---

<mds-usage variant={variant}>
  <mds-text typography="detail"><slot /></mds-text>
</mds-usage>
