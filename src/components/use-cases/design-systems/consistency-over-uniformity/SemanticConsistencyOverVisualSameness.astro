---
import UseCase from '../../../UseCase.astro';
import UseCaseInteractive from '../../../UseCaseInteractive.astro';
import Usage from '../../../Usage.astro';
import UsageItem from '../../../UsageItem.astro';
import Brand from '../../../Brand.astro';
import ProductCategory from '../../../ProductCategory.astro';
import Products from '../../../Products.astro';
---

<UseCase
  title="Semantic consistency over visual sameness"
  description="Similar UI elements were aligned semantically rather than visually."
>
  <UseCaseInteractive clsHeight={400} label="Semantic alignment" toggle>
    <mds-banner
      class="w-full max-w-9600"
      headline="Semantic consistency"
      icon="mi/outline/done"
      variant="primary"
    >
      <mds-text typography="caption">
        Different meanings should be represented by different component's variants.
      </mds-text>
      <mds-button variant="dark" tone="quiet" slot="action">Cancel</mds-button>
      <mds-button variant="primary" tone="strong" slot="action">Save changes</mds-button>
    </mds-banner>
    <mds-banner
      class="w-full max-w-9600"
      headline="Visual sameness"
      icon="mi/outline/close"
      variant="primary"
    >
      <mds-text typography="caption">
        Different meanings should be represented by different component's variants.
      </mds-text>
      <mds-button variant="primary" tone="strong" slot="action">Cancel</mds-button>
      <mds-button variant="primary" tone="strong" slot="action">Save changes</mds-button>
    </mds-banner>
  </UseCaseInteractive>

  <Usage>
    <UsageItem variant="do">
      Use the same component or pattern when the meaning and intent are the same.
    </UsageItem>
    <UsageItem variant="dont">
      Create visually identical components with different meanings.
    </UsageItem>
    <UsageItem variant="do">
      Let spacing, size, or hierarchy adapt to the surrounding layout.
    </UsageItem>
    <UsageItem variant="dont">Rely on appearance alone to communicate purpose.</UsageItem>
  </Usage>

  <Products>
    <ProductCategory title="Design tools">
      <Brand name="figma" label="Figma" />
    </ProductCategory>
    <ProductCategory title="Documentation">
      <Brand name="zeroheight" label="Zeroheight" />
    </ProductCategory>
  </Products>
</UseCase>
